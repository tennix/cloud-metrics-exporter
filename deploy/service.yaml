apiVersion: v1
kind: Service
metadata:
  name: cloud-metrics-exporter
  namespace: monitoring
spec:
  selector:
    app: cloud-metrics-exporter
  ports:
    - name: metrics
      port: 9100
      targetPort: metrics
